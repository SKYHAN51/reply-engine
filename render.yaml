services:
  - type: web
    name: reply-engine
    runtime: python
    rootDir: demo/api
    buildCommand: pip install -r ../../requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
